apiVersion: v1
kind: ConfigMap
metadata:
  name: workspace-contract
  labels:
    app.kubernetes.io/name: paw
    app.kubernetes.io/component: workspace
data:
  contract-version: v0
  profile: core
  state-policy: ephemeral
